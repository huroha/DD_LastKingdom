# 2026-03-24 세션 요약

## 작업 내용

### 1. EblaSystem 구현 완료 (Task 5~8)
- **Task 5**: `CombatUnit.TakeDamage`에서 `AddEbla(DEATHS_DOOR_EBLA)` 제거, 상수 `public const`로 변경 — 호출자(SkillExecutor)에서 EblaSystem 경유로 처리
- **Task 6**: `SkillExecutor`에 EblaSystem 연동 — 생성자에 EblaSystem 주입, 모든 `AddEbla` → `ModifyEbla` 전환, DeathsDoor 에블라 외부 처리 추가
- **Task 7**: `CombatStateMachine`에 EblaSystem 소유 — `m_AfflictionDebuff` SerializeField, 패스/아군사망/후반전 에블라 모두 `ModifyEbla` 경유, 200 사망 시 RemoveUnit + UnitDiedEvent 처리, 역순 순회로 리스트 변경 안전성 확보
- **Task 8**: `Affliction_Debuff` SO 에셋 생성, CombatManager 연결, 플레이 테스트 완료

### 2. EblaSystem 설계 보완 — 100 캡, 필멸의 약화, 회생 시스템
- **에블라 100 캡**: 100 미만에서 에블라 공격 시 100 초과 불가. Affliction 연출 후 초과분 무시
- **StatBlock 확장**: `damageMultiplier`(피해 배율 %), `eblaMultiplier`(에블라 피해 배율 %) 필드 추가, `Apply()`에 합산 반영
- **EblaSystem.ModifyEbla()**: 100 캡 클램프 + `eblaMultiplier` 적용
- **SkillExecutor.CalcDamageRange()**: `damageMultiplier` 적용 (CalcDamage + PreviewAttack 둘 다 반영)

### 3. 죽음의 문턱 디버프 시스템 구현
- **필멸의 약화 (DeathsDoor_Debuff)**: DeathsDoor 진입 시 자동 부여 — ACC -10, SPD -5, damageMultiplier -25, eblaMultiplier +33
- **회생 (DeathsDoor_Recovery)**: DeathsDoor에서 Alive 복귀 시 자동 부여 — ACC -2, SPD -1, damageMultiplier -5, eblaMultiplier +10. 원정 종료 전까지 영구 지속, 특수 캠핑 기술로만 제거
- SkillExecutor에 `ApplyDeathsDoorDebuff()` / `RemoveDeathsDoorDebuff()` 헬퍼 메서드 추가
- CombatStateMachine에 `m_DeathsDoorDebuff`, `m_DeathsDoorRecovery` SerializeField 추가

### 4. NikkeInfoPanel 스탯 색상 표시
- BaseStats와 CurrentStats 비교하여 버프(초록) / 디버프(빨강) / 기본(흰색) 색상 적용
- damageMultiplier 배율을 DMG 표시에 반영

## 설계 결정

- **에블라 100 캡**: 100 미만 구간에서 정확히 100에서 멈춤. Afflicted 상태(100 이상)에서는 캡 없이 200까지 누적
- **damageMultiplier / eblaMultiplier**: StatBlock에 float 필드로 추가, 가산 방식. `RecalculateStats`에서 합산 → 각 시스템에서 참조
- **디버프 중복 방지**: Recovery는 중복 체크 후 추가. DeathsDoor_Debuff는 Heal 시 제거되므로 중복 불가
- **damageMultiplier 표시**: CalcDamageRange에서 적용하여 CalcDamage/PreviewAttack/NikkeInfoPanel 모두 일관된 수치 표시

## 발견한 버그 / 수정

| 버그 | 원인 | 수정 |
|------|------|------|
| Recovery 디버프 DeathsDoor 반복 시 누적 | RemoveDeathsDoorDebuff에서 기존 Recovery 존재 체크 없이 매번 추가 | 추가 전 기존 Recovery 유무 체크 로직 추가 |
| ApplyCritEffects line 319 `target.AddEbla` 미변환 | Task 6에서 적 크리티컬 시 니케 에블라 부여 부분 누락 | 확인 필요 — `m_EblaSystem.ModifyEbla`로 변환 필요 |

## 다음 작업

- `ApplyCritEffects` line 319 `target.AddEbla(CRIT_EBLA_TO_ENEMY)` → `m_EblaSystem.ModifyEbla` 변환 확인
- SO 에셋 생성: `DeathsDoor_Debuff`, `DeathsDoor_Recovery` (Inspector)
- 전체 플레이 테스트: eblaMultiplier 동작, damageMultiplier 동작, 에블라 100 캡 + eblaMultiplier 상호작용, 에블라 200 영구 사망
- tasks.md `EblaSystem 구현` 항목 완료 처리
