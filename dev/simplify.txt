  ---
  네이밍/컨벤션

  3. EnemyData.cs — m_Hitsprite 네이밍 오류
  m_Hitsprite → m_HitSprite (m_PascalCase 컨벤션)

  4. CombatFieldView.cs — scaleoffset 지역 변수 네이밍
  float scaleoffset → float scaleOffset (camelCase)


  ---
  매직 넘버

  6. CombatFieldView.cs — sortingOrder 매직 넘버
  sr.sortingOrder = 10 - unit.SlotIndex; — 10을 const 또는 SerializeField로 분리.

  ---
  코드 정리

  7. DamagePopupPool.cs — SpawnEffect/SpawnDamage 파라미터 순서 불일치
  - SpawnDamage(pos, text, color, isNikke, scale) — isNikke가 4번째
  - SpawnEffect(pos, isNikke, text, color) — isNikke가 2번째
  통일 권장.

  8. NikkeData.cs — 프로퍼티 정렬
  CombatPrefab이 다른 Visual 프로퍼티와 분리되어 있음. AttackSprite, HitSprite, CombatPrefab을 한 블록으로.

  9. CombatFieldView.cs — FocusIn 캐싱 코드 중복
  m_AllLivingBuffer 루프와 m_FocusBuffer 루프가 동일한 패턴(ViewCache/OriginalScales/Positions/SortingOrders 저장). 헬퍼
   메서드로 추출 가능.